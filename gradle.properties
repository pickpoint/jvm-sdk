GROUP=io.pickpoint
POM_ARTIFACT_ID=pickpoint

# Keep legacy key for local scripts / README examples (version comes from VERSION file)
group=io.pickpoint

org.gradle.jvmargs=-Xmx1g
org.gradle.parallel=true
kotlin.code.style=official
